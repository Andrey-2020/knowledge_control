import { onAnswerCallback } from 'pages/Tests/Test';

/**
 * Выстраивание правильной последовательности
 */
export default function Sequense(args: {
  answers: string[],
  onAnswer: onAnswerCallback,
}): JSX.Element;
