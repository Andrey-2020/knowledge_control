import { onAnswerCallback } from 'pages/Tests/Test';

/**
 * Сопоставление элементов между собойываываы
 */
export default function Match(args: {
  answers: string[],
  onAnswer: onAnswerCallback,
}): JSX.Element;
